{
  "name": "aws-rest-with-dynamodb",
  "version": "1.0.0",
  "description": "Serverless CRUD service exposing a REST HTTP interface",
  "author": "",
  "license": "MIT",
  "scripts": {
    "postinstall": "pip3 install -r requirements.txt",
    "test": "sh tests/runAll.sh"
  },
  "devDependencies": {
    "serverless-python-requirements": "^5.0.1"
  }
}
